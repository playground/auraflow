# Published firmware binaries live here.
#
# Contents are tracked (the flasher serves them) but they're regenerated
# by `npm run publish:flasher`, which copies the four ESP-IDF outputs out
# of src/firmware/c/build/ and renames them to match manifest.json:
#   bootloader.bin, partitions.bin, ota_data_initial.bin, firmware.bin
